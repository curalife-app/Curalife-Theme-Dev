<div class="category-tabs">
  <div class="container">
    <ul>
      {% if section.settings.is_all %}<li><a href="#" data-collection-handle="all" class="active">All</a></li>{% endif %}

      {% for collection in section.settings.collections %}
        {% if isLink %}
          <li><a href="{{ collection.url }}" {% if request.path contains collection.handle %}class="active"{% endif %}>{{ collection.title }}</a></li>
        {% else %}
          <li><a href="#" data-collection-handle="{{ collection.handle }}" data-collection-description="{{ collection.description }}" {% if request.path contains collection.handle %}class="active"{% endif %}>{{ collection.title }}</a></li>
        {% endif %}
      {% endfor %}
    </ul>
  </div>
</div>

<style>
  .category-tabs ul {
    display: flex;
    flex-wrap: wrap;
    gap: 35px 10px;
    padding: 10px 0;
    justify-content: space-between;
  }

  .category-tabs ul li a {
    color: var(--light-green);
    padding: 10px 20px;
    border-radius: 20px;
    font-size: 20px;
    transition: all 0.3s ease-in-out;
  }

  .category-tabs ul li a.active {
    background: var(--vomit);
    color: var(--dark-green);
  }

  @media screen and (max-width: 768px) {
    .category-tabs.slide .container {
      --scrollbar-width: 0px;
      --mask-width: 20px;
      overflow-y: auto;
      width: 90vw;
      padding-right: var(--mask-width);
      --mask-image-content: linear-gradient(to right, transparent, white var(--mask-width), white calc(100% - var(--mask-width)), transparent);
      --mask-size-content: calc(100% - var(--scrollbar-width)) 100%;
      --mask-image-scrollbar: linear-gradient(white, white);
      --mask-size-scrollbar: var(--scrollbar-width) 100%;
      -webkit-mask-image: var(--mask-image-content), var(--mask-image-scrollbar);
      -webkit-mask-size: var(--mask-size-content), var(--mask-size-scrollbar);
      -webkit-mask-position: 0 0, 100% 0;
      -webkit-mask-repeat: no-repeat, no-repeat;
      mask-image: var(--mask-image-content), var(--mask-image-scrollbar);
      mask-size: var(--mask-size-content), var(--mask-size-scrollbar);
      mask-position: 0 0, 100% 0;
      mask-repeat: no-repeat, no-repeat;
    }

    .category-tabs ul {
      gap: 1vw;
      justify-content: center;
    }

    .category-tabs.slide ul {
      width: max-content;
    }

    .category-tabs ul li a {
      padding: 1vw 4vw;
      font-size: 5vw;
    }
  }
</style>


{% schema %}
  {
      "name": "Collections Menu",
      "disabled_on": {
        "groups": [
          "header",
          "footer"
        ]
      },
      "settings": [
        {
          "type": "collection_list",
          "id": "collections",
          "label": "Collections"
        },
        {
          "type": "checkbox",
          "id": "is_all",
          "label": "Show All Tab"
        },
        {
          "type": "checkbox",
          "id": "isLink",
          "label": "Show as links",
          "default": false
        }
      ],
      "presets": [
      {
        "name": "Collections Menu"
      }
    ]
  }
  {% endschema %}