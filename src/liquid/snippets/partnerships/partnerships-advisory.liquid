<section id="advisory-section">
    <div class="container">
        <h2 data-aos="fade-up">Our Business Advisory Board</h2>

        {% assign people =  "Mr. Edouard Culierman:Founder and Managing Partner of Catalyst Investments and serves as the Chairman of Cukierman & Co. Investment House. Since its establishment in 1993, Cukierman & Co. Investment House has been engaged in more than € 5.5 billion cross-border transactions across industries.
                            |Mr. Omer Cygler:Founder and managing partner of Lion Investment Partners. Omer has over 15 years of investment banking, advisory, capital markets and investing experience with disruptive internet and software companies."
                            | split: '|'%}

        <div class="people">
            {% for person in people %}
                {% capture thecycle %}{% cycle 'odd', 'even' %}{% endcapture %}
                <div class="person" data-aos="fade-{%if thecycle == 'odd'%}right{%else%}left{%endif%}">
                    <img class="decoration" src="{{ 'partnerships-advisory-person-decoration.png' | asset_url }}" alt="curalife advisory" loading="lazy">
                    <img class="avatar" src="{{ 'partnerships-advisory-person-' | append: forloop.index | append: ".png" | asset_url }}" alt="curalife advisory" loading="lazy">
                    <div class="person-content">
                        <h3>{{ person | split: ":" | first }}</h3>
                        <p>{{ person | split: ":" | last }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</section>

<style>
    #advisory-section .people {
        display: flex;
        justify-content: space-around;
    }

    #advisory-section .people .person {
        position: relative;
        background-color: var(--green-dark);
        width: 540px;
        height: 560px;
        padding: 30px;
        color: white;
        border-radius: 3px;
    }

    #advisory-section .people .person img.decoration {
        position: absolute;
        top: 0;
        right: 0;
    }

    #advisory-section .people .person img.avatar {
        position: relative;
        top: -60px;
        left: -80px;
    }

    #advisory-section .people .person .person-content {
        margin-top: -40px;
    }

    #advisory-section .people .person .person-content h3 {
        font-size: 30px;
    }

    @media screen and (max-width: 768px) {
        #advisory-section .people {
            flex-direction: column;
            gap: 15vw;
        }

        #advisory-section .people .person {
            width: 80%;
            height: unset;
            align-self: flex-end;
        }

        #advisory-section .people .person:nth-child(even) {
            align-self: flex-start;
        }

        #advisory-section .people .person img.avatar {
            width: 30vw;
        }

        #advisory-section .people .person:nth-child(even) img.avatar {
            left: 50vw;
        }

        #advisory-section .people .person:nth-child(even) .decoration {
            transform: scaleX(-1);
            right: unset;
            left: 0;
        }
    }
</style>