<div class="homepage-banner">
  <div class="container no-mobile-padding">
      <div class="bg">
          <img class="bg-vector" src="{{ 'homepage-new-year-banner-bg-vector.png' | asset_url }}" width="651" height="586" alt="">
          <img class="bg-img" src="{{ 'homepage-banner-bg.png' | asset_url }}" width="1313" height="566" alt="">
          <img class="bg-img-mobile" src="{{ 'homepage-new-year-banner-bg-mobile.png' | asset_url }}" width="375" height="626" alt="">
      </div>
      <div class="content">
          <h1>Effective.<br>Safe.<br>Natural.</h1>
          <p>Science based supplements that improve quality of life.</p>
          <div class="cta-button-wrapper">
              <img class="bottle-img" src="{{ 'curalin-bottle.png' | asset_url }}" width="200" height="211" alt="Curalin Bottle">
              <a class="cta-btn" href="/collections">Shop Now</a>
              <div class="free-shipping">
                   {% comment %} <img class="free-shipping-icon" src="{{ 'icon-free-shipping-light-white.png' | asset_url }}" width="44" height="25" alt=""> {% endcomment %}
                  <p>Worldwide Shipping</p>
              </div>
          </div>
      </div>
      <div class="header-mobile-bottom">
          <div class="free-shipping">
              <p>Worldwide Shipping • 60-Day Money Back Guarantee</p>
          </div>
      </div>
  </div>
</div>
<style>
  .homepage-banner {
    background-color: #E8E8B0;
    position: relative;
    height: 566px;
    overflow: hidden;
  }

  .homepage-banner .bg .bg-vector {
    position: absolute;
    z-index: 2;
    max-height: 100%;
  }

  .homepage-banner .bg .bg-img-mobile {
    display: none;
  }

  .homepage-banner .bg .bg-img {
    position: absolute;
    z-index: 0;
    right: -220px;
    width: 75vw;
    height: 566px;
    object-fit: cover;
  }

  .homepage-banner .content {
    position: relative;
    z-index: 10;
    color: var(--dark-green);
    font-size: 50px;
    font-weight: 600;
    position: relative;
    z-index: 10;
    padding: 30px 15px;
  }

  .homepage-banner .content h1 {
    font-size: 50px;
    font-weight: 600;
    text-transform: uppercase;
  }

  .homepage-banner .content p {
    font-size: 30px;
    max-width: 450px;
    font-weight: 300;
  }

  .homepage-banner .content .cta-button-wrapper {
    position: relative;
  }

  .homepage-banner .content .cta-button-wrapper .bottle-img {
    width: 200px;
    margin-left: -50px;
    z-index: 1;
    position: relative;
    pointer-events: none;
  }

  .homepage-banner .content .cta-button-wrapper .cta-btn {
    text-transform: uppercase;
    background: #404733 ;
    box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
    border-radius: 10px;
    color: white;
    padding: 10px 30px;
    font-size: clamp(30px, 2vw, 38px);
    margin-left: -75px;
    border: 0;
    transition: background 0.3s ease-in-out;
  }

  .homepage-banner .content .cta-button-wrapper .cta-btn:hover {
    background: var(--dark-green);
  }

  .homepage-banner .content .cta-button-wrapper .free-shipping {
    display: flex;
    align-items: center;
    gap: 10px;
    position: absolute;
    left: 105px;
    top: 150px;
  }

  .homepage-banner .content .cta-button-wrapper .free-shipping .free-shipping-icon {
    height: 25px;
  }

  .homepage-banner .content .cta-button-wrapper .free-shipping p {
    font-weight: bold;
    font-size: 20px;
    margin: 0;
  }

  .homepage-banner .header-mobile-bottom {
    display: none;
  }

  @media screen and (max-width: 1200px) {
    .homepage-banner .bg .bg-img {
      right: 0px;
      width: 70vw;
    }
  }

  @media screen and (max-width:1270px) {
    .homepage-banner .bg .bg-vector {
      margin-left: -150px;
    }

    .homepage-banner .bg .bg-img {
      object-position: left;
    }
  }

  @media screen and (max-width:768px) {
    .homepage-banner .content .cta-button-wrapper .cta-btn {
      margin-left: 0;
      font-size: 4vw;
    }

    .homepage-banner .bg .bg-vector {
      display: none;
    }

    .homepage-banner .bg .bg-img-mobile {
      display: block;
      position: absolute;
      width: 100%;
    }

    .homepage-banner .bg .bg-img {
      display: none;
    }

    .homepage-banner .header-mobile-bottom {
      display: block;
      position: absolute;
      bottom: 0;
      width: 100%;
      background: #E4E4AB;
    }

    .homepage-banner .header-mobile-bottom .free-shipping {
      color: var(--dark-green);
      display: flex;
      gap: 10px;
      align-items: center;
      justify-content: center;
      font-size: 3vw;
      font-weight: bold;
    }

    .homepage-banner .content .cta-button-wrapper .free-shipping {
      display: none;
    }

    .homepage-banner .header-mobile-bottom .free-shipping .free-shipping-icon {
      height: 5vw;
    }

    .homepage-banner {
      height: 180vw;
    }

    .homepage-banner .content .cta-button-wrapper .bottle-img {
      display: none;
    }

    .homepage-banner .content h1 {
      font-size: 7vw;
      margin: 0;
    }

    .homepage-banner .content p {
      font-size: 5vw;
      margin: 4vw 0;
    }

    .homepage-banner .content .cta-button-wrapper button {
      margin-left: 0;
      font-size: 7vw;
    }
  }
</style>