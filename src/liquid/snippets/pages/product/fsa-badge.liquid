<!-- Pay with HSA Badge -->
<div class="pay-with-hsa-badge">
  Pay with HSA | FSA <a href="#" id="hsa-badge-link">Read More</a>
</div>

<!-- HSA Modal -->
<div id="hsa-modal" class="modal" aria-labelledby="hsa-modal-title" aria-hidden="true">
  <div class="modal-backdrop"></div>
  <div class="modal-content">
    <div class="close-icon" aria-label="Close modal">&times;</div>
    <h2 id="hsa-modal-title">How does it work?</h2>
    <div id="hsa-modal-content">
      <div class="step">
        <div class="step-number">1</div>
        <div class="step-content">
          <p>Select "Buy Once" and choose your preferred supply option.</p>
          <div class="step-image">
            <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/how-fsa-work-1.png" alt="Buy Once option">
          </div>
        </div>
      </div>
      <div class="step">
        <div class="step-number">2</div>
        <div class="step-content">
          <p>At checkout, choose the payment method "Flex | Pay with HSA/FSA".</p>
          <div class="step-image">
            <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/how-fsa-work-2.png" alt="Payment method selection">
          </div>
        </div>
      </div>
      <div class="step">
        <div class="step-number">3</div>
        <div class="step-content">
          <p>Complete the Intake Form – this is a one-time process.</p>
          <div class="step-image">
            <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/how-fsa-work-3.png" alt="Intake Form">
          </div>
        </div>
      </div>
      <div class="step">
        <div class="step-number">4</div>
        <div class="step-content">
          <p>Enter your HSA/FSA card details and place your order.</p>
          <div class="step-image">
            <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/how-fsa-work-4.png" alt="Card details entry">
          </div>
        </div>
      </div>
      <div class="step">
        <div class="step-number">5</div>
        <div class="step-content">
          <p>Once approved, the approval is valid for 1 year from the date of your first HSA/FSA order.*</p>
          <div class="step-image">
            <img src="https://cdn.shopify.com/s/files/1/0452/2459/1511/files/how-fsa-work-5.png" alt="Approved">
          </div>
        </div>
      </div>
    </div>
    <button class="close-button">Close</button>
  </div>
</div>

<style>
/* Pay with HSA Badge Styles */
.pay-with-hsa-badge {
  display: inline-block;
  background-color: #E5E3FF;
  border-radius: 15px;
  font-weight: bold;
  padding: 0.5rem 2rem;
}

.pay-with-hsa-badge a {
  text-decoration: underline;
  font-style: italic;
  font-weight: normal;
  color: #0D007C;
  margin-left: 0.5rem;
}

/* Modal Styles */
.modal {
  --border: #e2e8f0;
  --bg: #f8f7ff;
  position: fixed;
  inset: 0;
  z-index: 1000;
  overflow: auto;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.3s, visibility 0.3s;
}

.modal.show {
  opacity: 1;
  visibility: visible;
}

.modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.5);
  opacity: 0;
  transition: opacity 0.3s;
}

.modal.show .modal-backdrop {
  opacity: 1;
}

.hsa-modal-content {
  display: flex;
  flex-wrap: wrap;
  gap: 3%;
}

.modal-content {
  background: var(--bg);
  margin: 3% auto;
  padding: 2rem;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  max-width: 1200px;
  width: 90%;
  transform: translateY(-20px);
  opacity: 0;
  transition: transform 0.3s, opacity 0.3s;
  position: relative;
}

.modal.show .modal-content {
  transform: translateY(0);
  opacity: 1;
}

.modal-content h2 {
  margin-bottom: 2rem;
  text-align: center;
}

.step {
  display: flex;
  align-items: flex-start;
  margin-bottom: 2rem;
  width: 30%;
}

.step-number {
  background-color: var(--primary-color);
  color: white;
  width: 2rem;
  height: 2rem;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  margin-right: 1rem;
  flex-shrink: 0;
}

.step-content {
  flex-grow: 1;
}

.step-content p {
  margin-bottom: 1rem;
  color: var(--text);
}

.step-image {
  border: 1px solid var(--border);
  border-radius: 8px;
  overflow: hidden;
}

.step-image img {
  max-width: 100%;
  height: auto;
  display: block;
}

.approved-badge {
  display: inline-flex;
  align-items: center;
  background-color: #e0f2fe;
  color: #0284c7;
  padding: 0.5rem 1rem;
  border-radius: 9999px;
  font-weight: bold;
}

.approved-badge svg {
  width: 1.5rem;
  height: 1.5rem;
  margin-right: 0.5rem;
}

.close-icon {
  position: absolute;
  top: 1rem;
  right: 1rem;
  font-size: 1.5rem;
  background: none;
  border: none;
  cursor: pointer;
  color: var(--primary-color);
  transition: color 0.3s;
}

.close-button {
  display: block;
  width: auto;
  margin: 2rem auto 0;
  padding: 0.75rem 2rem;
  background-color: var(--primary-color);
  color: white;
  border: none;
  border-radius: 9999px;
  font-size: 1rem;
  cursor: pointer;
  transition: background-color 0.3s;
}

@media (max-width: 640px) {
  .modal-content {
    padding: 1.5rem;
  }

  .modal-content h2 {
    font-size: 1.5rem;
  }

  .step {
    flex-direction: column;
  }

  .step-number {
    margin-bottom: 0.5rem;
  }
}
</style>

<script>
// Get the modal element
const hsaModal = document.getElementById('hsa-modal');

// Function to toggle the modal visibility
const toggleHSAModal = (show) => {
  hsaModal.classList.toggle('show', show);
  hsaModal.setAttribute('aria-hidden', !show);
  document.body.style.overflow = show ? 'hidden' : '';
};

// Open modal when badge is clicked
document.getElementById('hsa-badge-link').addEventListener('click', (event) => {
  event.preventDefault();
  toggleHSAModal(true);
});

// Close modal when close icon or button is clicked
hsaModal.querySelector('.close-icon').addEventListener('click', () => toggleHSAModal(false));
hsaModal.querySelector('.close-button').addEventListener('click', () => toggleHSAModal(false));

// Close modal when clicking outside the modal content
hsaModal.addEventListener('click', (event) => {
  if (event.target === hsaModal || event.target.classList.contains('modal-backdrop')) {
    toggleHSAModal(false);
  }
});

// Close modal when pressing the Escape key
document.addEventListener('keydown', (event) => {
  if (event.key === 'Escape' && hsaModal.classList.contains('show')) {
    toggleHSAModal(false);
  }
});
</script>