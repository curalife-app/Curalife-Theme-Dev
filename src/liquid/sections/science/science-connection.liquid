<div class="container">
  <h2 class="connection-title text-center">{{ section.settings.title }}</h2>

  <div class="both-connection-wrapper">
    <div class="connection-text">
      <h4>{{ section.settings.subtitle_1 }}</h4>
      <div class="connection-paragrath">{{ section.settings.main_content_1 }}</div>
    </div>

    <div class="connection-wrapper-img">
      <img src="{{ section.settings.image_1 | image_url }}" width="572" height="444" alt="">
    </div>
  </div>
  <div class="both-connection-wrapper-2">
    <div class="connection-wrapper-img-2">
      <img src="{{ section.settings.image_2 | image_url }}" width="572" height="444" alt="">
    </div>
    <div class="connection-text">
      <h4>{{ section.settings.subtitle_2 }}</h4>
      <div class="connection-paragrath">{{ section.settings.main_content_2 }}</div>
    </div>
  </div>
</div>

<style>
  .connection-i-t {
    background: {{ section.settings.bg_color }};
    padding-bottom: unset;
  }

  .both-connection-wrapper {
    display: flex;
    align-items: center;
    gap: 50px;
    padding: 0 20px;
  }

  .both-connection-wrapper-2 {
    display: flex;
    align-items: center;
    gap: 50px;
    padding: 20px 20px 0;
  }

  .connection-text {
    max-width: 50%;
  }

  .connection-wrapper-img-2 {
    display: flex;
    align-items: flex-end;
  }


  @media screen and (max-width: 768px) {
    .connection-title {
      padding:4vw;
    }

    .both-connection-wrapper {
      flex-direction: column;
    }

    .both-connection-wrapper-2 {
      flex-direction: column-reverse;
      padding: 20px 20px 0;
    }

    .both-connection-wrapper-2 img {
      margin-left: -40vw;
      min-width: 150%;
    }

    .connection-text {
      max-width: unset;
    }
  }
</style>

{% schema %}
{
  "name": "Connection I&T",
  "tag": "section",
  "class": "connection-i-t",
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  },
  "settings": [{
      "type": "color",
      "id": "bg_color",
      "label": "Background Color"
    },
    {
      "type": "image_picker",
      "id": "image_1",
      "label": "Image 1"
    },
    {
      "type": "image_picker",
      "id": "image_2",
      "label": "Image 2"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "text",
      "id": "subtitle_1",
      "label": "Subtitle 1"
    },

    {
      "type": "text",
      "id": "subtitle_2",
      "label": "Subtitle 2"
    },

    {
      "type": "richtext",
      "id": "main_content_1",
      "label": "Content 1"
    },

    {
      "type": "richtext",
      "id": "main_content_2",
      "label": "Content 2"
    }

  ],
  "presets": [{
    "name": "Connection I&T"
  }]
}
{% endschema %}