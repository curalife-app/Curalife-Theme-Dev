{% layout 'theme.lps' %}

{% render 'lp-community-header-origin' %}
{% render 'lp-community-hero-origin' %}
{% render 'lp-community-statistics' %}
{% render 'lp-community-people' %}
{% render 'lp-community-cookbook' %}
{% render 'lp-community-food' %}
{% render 'lp-community-advice' %}
{% render 'lp-community-topics' %}
{% render 'lp-community-articles' %}
{% render 'lp-community-infographic' %}
{% render 'lp-community-moderators' %}
{% render 'lp-community-education' %}

<script>
  function getGeo() {
    $.get("https://geo.curalife.com/", function(data) {
      let country = JSON
        .parse(data)
        .country;
      $('[data-name="lead_geo"]').val(country);
    });
  }

  function getOperatingSys() {
    var os = 'unknown';
    if (navigator.userAgent.indexOf("Win") != -1) os = "Windows";
    if (navigator.userAgent.indexOf("Mac") != -1) os = "MacOS";
    if (navigator.userAgent.indexOf("X11") != -1) os = "UNIX";
    if (navigator.userAgent.indexOf("Linux") != -1) os = "Linux";
    return os;
  }

  const getDeviceType = () => {
    let ua = navigator.userAgent;
    if (/(tablet|ipad|playbook|silk)|(android(?!.*mobi))/i.test(ua)) {
      return "tablet";
    }
    if (/Mobile|iP(hone|od)|Android|BlackBerry|IEMobile|Kindle|Silk-Accelerated|(hpw|web)OS|Opera M(obi|ini)/.test(ua)) {
      return "mobile";
    }
    return "desktop";
  };

  window.onload = function() {
    let queryString = window.location.search;
    let urlParams = new URLSearchParams(queryString);
    $('[data-name="lead_source"]').each(function() {$(this).val("CommunityLP")});
    $('[data-name="lead_utm_source"]').each(function() {$(this).val(urlParams.get('utm_source'))});
    $('[data-name="lead_utm_medium"]').each(function() {$(this).val(urlParams.get('utm_medium'))});
    $('[data-name="lead_utm_campaign"]').each(function() {$(this).val(urlParams.get('utm_campaign'))});
    $('[data-name="lead_utm_term"]').each(function() {$(this).val(urlParams.get('utm_term'))});
    $('[data-name="lead_utm_content"]').each(function() {$(this).val(urlParams.get('utm_content'))});
    $('[data-name="lead_source_date"]').each(function() {$(this).val(new Date().toUTCString())});
    $('[data-name="lead_os"]').each(function() {$(this).val(getOperatingSys())});
    $('[data-name="lead_device"]').each(function() {$(this).val(getDeviceType())});
    $('[data-name="lead_url"]').each(function() {$(this).val(window.location.href)});
    getGeo();
  }
</script>

<style>
  .wisepops-root {
    display: none;
  }

  body {
    padding-top: unset;
  }

  .container {
    position: relative;
  }

  main section {
    padding-bottom: 50px;
    text-align: center;
    position: relative;
  }

  main section:nth-of-type(even) {
    background-color: #dfdfdf;
  }

  main section:nth-of-type(odd) {
    background-color: #F2F3F6;
  }

  h2 {
    text-align: center;
    font-weight: 500;
    font-size: 48px;
    line-height: 65px;
  }

  p {
    font-size: 24px;
    font-weight: 300;
    line-height: 35px;
    text-align: start;
  }

  form.sign-form {
    display: flex;
    flex-direction: column;
    width: 400px;
  }

  button {
    background: #FFC700;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
    border: 0;
    border-radius: 6px;
    color: white;
    font-size: 45px;
    padding: 15px 30px;
    margin-top: 50px;
  }

  form button {
    margin-top: 20px;
    font-size: 28px;
    font-weight: normal;
  }

  input {
    border: 1px solid grey;
    border-radius: 6px;
    font-size: 23px;
    text-align: center;
    padding: 15px 30px;
    margin-top: 20px;
  }

  input::placeholder {
    opacity: 0.3;
  }

  .swiper-nav {
    background-color: white;
    width: 60px;
    height: 60px;
    border-radius: 50%;
    box-shadow: 4px 4px 10px rgb(0 0 0 / 25%);
    color: black;
  }

  .swiper-nav::after {
    font-size: 30px;
    font-weight: bold;
  }

  .swiper-nav.disabled {
    display: none;
  } 

  @media screen and (max-width: 991px) {
    .container {
      padding: 0 20px;
    }

    h2 {
      text-align: start;
      font-size: 5.8vw;
      line-height: 7vw;
    }

    p {
      font-size: 5vw;
      line-height: 7vw;
    }

    button {
      font-size: 7vw;
    }

    form button {
      font-size: 6vw;
    }

    img.section-badge {
      width: 30vw;
    }

    form.sign-form {
      width: 100%;
    }
  }
</style>
